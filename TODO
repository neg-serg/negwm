subtle like semi-dynamic tiling / dwm-like tiling
add labels support: dynamic workspaces(for example print scratchpad name)
better attach ws / ns menu
add https://www.python.org/dev/peps/pep-0519/
share some functions like window::focus between various mods
new module idea: mouse helper, for example get windows size and then click on x,y = 20px, 20px
add executor run check via something like dash -c
percent resize
